"use client";

import LandingPage from './_widgets/landing';

export default function HomePage() {
    // const t = useTranslations('HomePage');
    return (
        <div className='relative w-full h-screen flex flex-col items-center justify-center '>
            <LandingPage />
            
        </div>
    );
}